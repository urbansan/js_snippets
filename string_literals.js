function costam(str, param, param2){
	console.log({ str, param, param2});
	console.log(str.join({ param, param2 }));
};

hadouken = 'kalafiorowa';
sle = 'sledziowa';

costam`miala Ci ja pieczen ${hadouken} i ${sle}`;

