'use struct';
const pes = document.querySelectorAll('p');

classList = ['success', 'error']
// console.log({ classList.prototype });

// obj = {a: 10, b: 20}
// obj.
// console.log(obj.)

// console.log(elements(classList))
// for(let i in pes.keys()){
//     console.log(i, pes[i], typeof pes[i]);
// }
// pes.forEach(param => {
//     // console.log(typeof param, param);
//     for(let class_ in classList){
//         console.log({ param, class_});
//         if(param.innerText.includes(class_)){
//             param.className = class_;
//         }
//     }
// })

console.log('hadouken' || 'siemano');

// pes.forEach(param => {
//     classList.forEach(class_elm => {
//         if(param.innerText.includes(class_elm)){
//             param.className = class_elm;
//         }
//     });
// });

